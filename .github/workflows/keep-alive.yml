name: Keep Bot Awake

on:
  schedule:
    - cron: "*/5 * * * *"  # run every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render bot
        run: |
          curl -s "https://gainaccess-bot.onrender.com/" || exit 0
